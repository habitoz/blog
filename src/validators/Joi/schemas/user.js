import Joi from 'joi';

const addUser = Joi.object().keys({
       
});

const updateUser = Joi.object().keys({
       
});

const statusChange = Joi.object().keys({
       
});

export default {
    addUser,
    updateUser,
    statusChange
};